# Task 001 — Content & IA — progress log

## Status: completed

## Content shape chosen: plain TypeScript data modules under `src/data/`, NOT Astro content collections

No `src/content/**` files were created (that directory still only has `.gitkeep`).
Everything lives in `src/data/*.ts` as typed exports. Rationale: this content
is short structured prose + metrics, not markdown-heavy documents, so plain
typed objects are simpler for 002's templates to consume than a content-
collection schema + `getCollection()` indirection, and give 002 compile-time
safety for every field.

### Files 002 should import (import contract)

- **`src/data/site.ts`** — `identity`, `nav`, `skipLinkLabel`, `hero`
  (h1/lead/metaLine/ctaPrimary/ctaSecondary/ctaTertiary/portraitAlt), `about`
  (string[] of paragraphs), `contact`, `footer`, `otherWorkHeading`,
  `notFound`. `hero.lead` is the verbatim §3 headline sentence — do not
  paraphrase it in the template.
- **`src/data/projects.ts`** — `featuredProjects: FeaturedProject[]` (already
  sorted by `order` 1–5: dishpatch, gpt2-from-scratch,
  deep-learning-statistical-arbitrage, idx-alpha, maintain-ai) and
  `otherWork: OtherWorkItem[]`. Use `featuredProjects` directly for both the
  home cards and to generate the five `/projects/[slug]/` static paths
  (`slug` field matches epic §5.2 exactly). Each project has `cardMetrics`
  (for the home card) and `detailMetrics` (for the detail page — not always
  identical, e.g. IDX-alpha's detail strip adds the CV protocol). `whatItIs`
  and `whatIDid` are `string[]` (one entry per paragraph); `whatIFound` is a
  single string. `repo` is the detail-page repo link; `extraLinks` holds
  DishPatch's two PR links and GPT-2's nanoGPT attribution link — render
  these on the detail page only, not the card. IDX-alpha has `status: 'wip'`
  and `statusLabel: 'Work in progress'` — render this as a visible label on
  both the card and the detail page (epic §6.1 item 4, acceptance criterion
  22 — this was flagged as the easiest one to get wrong).
- **`src/data/cv.ts`** — `cvH1`, `cvSummary`, `cvContact`/`cvContactLine`
  (note: lowercase "open" here, deliberately different casing from
  `site.ts`'s `heroMetaLine` — both are verbatim per their respective epic
  sections, do not unify them), `downloadCvLabel`, `experience`,
  `education`, `skills`, `achievements`. For the CV page's brief "Projects"
  section, use `featuredProjects[].cvOneLiner` + `featuredProjects[].slug`
  to link out — do NOT duplicate the detail-page paragraphs on the CV page
  (§16 item 8).
- **`src/data/meta.ts`** — `pageMeta.home` / `pageMeta.cv` / `pageMeta.notFound`
  (`{title, description}`) for `<head>` on those three pages. Project detail
  pages use `metaTitle`/`metaDescription` fields directly on each
  `FeaturedProject` instead. `siteMetaDefaults` has the OG image path/alt —
  **the OG image asset itself doesn't exist yet** (§16 item 6, delegated);
  `ogImagePath: '/og-image.png'` is a placeholder path, update it if
  002/004 land the asset somewhere else.
- **`src/data/seo.ts`** — `robotsTxt` (plain string). File placement is
  004's territory (`public/**`); I don't own that directory, so I didn't
  write `public/robots.txt` myself. See "Dependencies needed" below re:
  sitemap.

## §16 judgment calls made

- **§16.4 (card layout)** — not resolved here, that's 002's call; I only
  guaranteed all 5 projects are present with no pagination/filtering
  dependency in the data shape.
- **§16.8 (CV duplication)** — resolved: added a dedicated `cvOneLiner`
  field per project distinct from `cardSummary`, so the CV page's one-liner
  can never accidentally diverge into a duplicated paragraph.
- **Project "dates" for IDX-alpha and maintain-ai** — epic.md doesn't give
  an explicit start date for either (only IDX-alpha's *data coverage* range
  and a 2026-08-17 status-check date are given, and maintain-ai's README
  has no dates at all). Rather than invent a date, I used the literal string
  `'Ongoing'` for both. This is a status word, not a fabricated fact.
- **maintain-ai metrics** — fetched the README from
  `https://github.com/hadissuryaalamin/maintain-ai` via WebFetch on
  2026-08-17 per the task brief (epic explicitly defers this project's
  numbers to the README). Verified facts used: six typed tools (manual
  search, machine-status inspection, sensor-history retrieval, incident
  search, downtime-cost calculation, work-order drafting); 30-case synthetic
  eval; task completion 53.3% → 60.0%; correct tool selection 86.7% → 93.3%;
  token usage −43.0%; stack FastAPI/Streamlit/SQLite/OpenAI Responses
  API/MCP/Docker Compose/GitHub Actions. No Schneider Electric mention in
  the README itself — that connection is explicit editorial framing required
  by epic §6.1 item 5, written into `whatIDid`, not sourced from the repo.
- **DLSA negative result** — framed per the epic's exact instruction: the
  client didn't initially understand what the factor model was doing, and
  the negative result (GNN adds nothing beyond factor structure) is
  presented in `whatIFound` as rigour/learning, not failure or apology.
- **IDX-alpha Sharpe discipline** — the only Sharpe figure anywhere in the
  data is `3.12` (after 25 bps costs) inside `whatIFound`'s body prose, with
  the cost assumption stated and the README's own "not a deployable edge at
  face value" caveat quoted next to it. `3.62` does not appear anywhere in
  the data (safer than including it non-alone). No Sharpe figure exists in
  `cardSummary`, `cardMetrics`, or `detailMetrics` for this project.

## Verification performed

- Grepped all of `src/data/` for: 9+ consecutive digits, the AU mobile
  regex from epic §12 acceptance criterion 27, the seven tailored-CV company
  names, the three COMP4020 prototype names, and street/postcode markers —
  zero matches in rendered data (the only "Aumovio" hits are in `cv.ts`'s
  own doc-comments explaining the exclusion; comments aren't emitted to
  `dist/`).
- Confirmed both verbatim strings (§3 headline sentence, hero meta line) and
  the CV page's differently-cased contact line match epic.md byte-for-byte
  via a Node string-includes check.
- Parsed every new file with `node --experimental-strip-types` to confirm
  valid TypeScript syntax and that `featuredProjects` has exactly 5 entries
  in the correct §6.1 order.

## Dependencies needed (for the coordinating session to batch-install)

- `@astrojs/sitemap` — if Phase-3 sitemap.xml generation should be
  automatic rather than hand-written. Not installed; `src/data/seo.ts` only
  ships `robots.txt` content for now, pointing at `/sitemap-index.xml`
  (the path `@astrojs/sitemap` would produce by default).
- Not a new dependency, but a coordination note: `node_modules/.bin/astro
  check` prompted to install `@astrojs/check` + `typescript` interactively —
  I did not accept that prompt (no installs performed, `package.json`/lockfile
  untouched). If task 005 wants `astro check` as part of `pnpm check`, it
  will need those two packages added to the batch install too.

## Nothing outstanding for this task

All of `src/content/**` (unused by design) and `src/data/{site,projects,cv,meta,seo}.ts`
are complete. I did not touch any `.astro` file. Ready for 002 to build
`src/pages/index.astro`, `src/pages/cv/index.astro`,
`src/pages/projects/[slug].astro`, and `src/pages/404.astro` against this
data.
